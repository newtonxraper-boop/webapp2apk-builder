name: Generate Persistent Keystore (DEPRECATED - do not run)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Explain why this workflow no longer does anything
        run: |
          echo "This workflow used to commit android-template/app/debug.keystore"
          echo "directly to this PUBLIC repository, with the well-known password"
          echo "'android'/'android'. That meant anyone who found it could sign"
          echo "their own APK with the exact same signature and have Android"
          echo "accept it as a trusted update to any app built from this repo."
          echo ""
          echo "It has been disabled. Use the 'Generate Release Keystore"
          echo "(one-time setup)' workflow instead - it creates a real keystore"
          echo "with random passwords and never commits it anywhere."
          exit 1
